spring:
  application:
    name: ConnectSphere
  datasource:
    url: jdbc:postgresql://localhost:5433/connectsphere
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: connectSphereSuperSecretKeyConnectSphere12345
  expiration: 86400000 # 1 gün
